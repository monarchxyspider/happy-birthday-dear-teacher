// Optional image configuration.
// This app currently uses icons and CSS only, so no custom image is required.
// Add image URLs here later, for example:
// const CUSTOM_IMAGES = { logo: "https://example.com/logo.png" };
window.HEM_CONFIG = {
  CUSTOM_IMAGES: {},
};